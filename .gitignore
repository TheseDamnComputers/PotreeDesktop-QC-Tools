node_modules
libs/Cesium

# package-lock.json is deliberately committed: it pins the exact dependency tree
# so npm install resolves the same way on every machine. Upstream ignores it.

# Point cloud data and conversion output - never commit these
*.las
*.laz
*_converted/
*_converted_*/

# Machine-specific launcher (absolute paths); create your own
*.lnk

# Personal notes and scratch - never published
notes/
